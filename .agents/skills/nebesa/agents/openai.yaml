interface:
  display_name: "Nebesa"
  short_description: "Repo-specific patterns and workflows for nebesa"
  default_prompt: "Use the nebesa repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true